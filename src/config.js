// Supabase credentials — injected from GitHub Actions secrets during deploy.
// When both are empty, high-score features are silently disabled (local dev default).
export const SUPABASE_URL = '';
export const SUPABASE_ANON_KEY = '';
